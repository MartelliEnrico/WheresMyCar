name: "CodeQL"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: "30 16 * * 1"

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    timeout-minutes: 360
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false

    steps:
    - name: Checkout repository
      uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3

    - name: Initialize CodeQL
      uses: github/codeql-action/init@407ffafae6a767df3e0230c3df91b6443ae8df75 # v2
      with:
        languages: "java-kotlin"
        queries: security-and-quality

    - name: Set Up JDK
      uses: actions/setup-java@0ab4596768b603586c0de567f2430c30f5b0d2b0 # v3
      with:
        distribution: zulu
        java-version: 21
        cache: gradle

    - name: Change wrapper permissions
      run: chmod +x ./gradlew

    - name: Create local.properties
      env:
        MAPS_API_KEY: ${{ secrets.MAPS_API_KEY }}
      run: echo MAPS_API_KEY="$MAPS_API_KEY" > ./local.properties

    - name: Build
      run: ./gradlew build

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@407ffafae6a767df3e0230c3df91b6443ae8df75 # v2
      with:
        upload: false
        output: sarif-results

    - name: Filter SARIF
      uses: advanced-security/filter-sarif@f3b8118a9349d88f7b1c0c488476411145b6270d # v1
      with:
        patterns: |
          -.gradle/**
        input: sarif-results/java-kotlin.sarif
        output: sarif-results/java-kotlin.sarif

    - name: Upload SARIF
      uses: github/codeql-action/upload-sarif@407ffafae6a767df3e0230c3df91b6443ae8df75 # v2
      with:
        sarif_file: sarif-results/java-kotlin.sarif
        category: "/language:java-kotlin"
